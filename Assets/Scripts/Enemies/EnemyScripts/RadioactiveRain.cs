using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RadioactiveRain : ProjectileScript
{
    // Start is called before the first frame update
    void Start()
    {
         destroyTime = 5f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
